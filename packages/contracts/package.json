{
  "name": "@stealth-card/contracts",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "build:circuits": "bash script/build-circuits.sh",
    "deploy:base-sepolia": "source ../../.env && forge script script/Deploy.s.sol --rpc-url base_sepolia --broadcast --verify",
    "deploy:arb-sepolia": "source ../../.env && forge script script/Deploy.s.sol --rpc-url arb_sepolia --broadcast --verify"
  },
  "devDependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.4"
  }
}
